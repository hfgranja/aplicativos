# Database
DATABASE_URL=sqlite:///./receivables.db

# CORS
FRONTEND_ORIGIN=http://localhost:5173

# Simulation
# Speeds up the demo: seconds before a simulated provider resolves an attempt
SIM_MIN_RESOLUTION_SECONDS=2
SIM_MAX_RESOLUTION_SECONDS=6
